import styled from "@emotion/styled";

export const Wrapper = styled.div`
    max-width: 700px;

    padding: 1.25rem;

    border-radius: 2.5rem;
    background-image: linear-gradient(
        20deg,
        rgba(16, 31, 50, 1) 0%,
        rgba(127, 117, 240, 1) 100%
    );
`;
